apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      imagePullSecrets:
        - name: ghcr-registry-auth
      containers:
        - name: myapp
          image: "ghcr.io/placeholder/placeholder"
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
