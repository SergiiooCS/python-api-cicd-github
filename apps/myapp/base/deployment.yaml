apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: myapp
          image: ghcr.io/sergiioocs/python-api-cicd-testing:main-fe1dd4f-1777473468 # {"$imagepolicy": "flux-system:myapp"}
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
